function showLoader(){let loader=document.getElementById('loader');if(!loader){loader=document.createElement('div');loader.id='loader';loader.textContent=''}if(!document.body.contains(loader)){document.body.appendChild(loader)}document.body.classList.add('no-scroll');document.documentElement.classList.add('no-scroll','ajax-loading');loader.style.transition='opacity 0.15s ease-out';if(getComputedStyle(loader).opacity===''||getComputedStyle(loader).opacity==='1'){loader.style.opacity='0'}requestAnimationFrame(()=>{loader.style.opacity='1'})}function hideLoader(){const loader=document.getElementById('loader');if(!loader){return}loader.style.transition='opacity 0.15s ease-out';loader.style.opacity='0';setTimeout(()=>{loader.remove();document.body.classList.remove('no-scroll');document.documentElement.classList.remove('no-scroll','ajax-loading')},150)}function watchLogo(img){const done=()=>waitHeroBg(hideLoader);if(img.decode){img.decode().then(done,done)}else if(img.complete){done()}else{img.addEventListener('load',done,{once:true});img.addEventListener('error',done,{once:true})}}function waitHeroBg(callback){const hero=document.getElementById('hero');if(!hero){return callback()}const match=getComputedStyle(hero).backgroundImage.match(/url\(["']?(.*?)["']?\)/);if(!match){return callback()}const img=new Image();img.src=match[1];if(img.complete){return callback()}img.addEventListener('load',callback,{once:true});img.addEventListener('error',callback,{once:true})}document.addEventListener('DOMContentLoaded',()=>{const buttons=document.querySelectorAll('.collection-btn, .solution-btn');buttons.forEach(btn=>{btn.addEventListener('click',event=>{const isActive=btn.classList.contains('active');if(!isActive){const targetId=btn.classList.contains('collection-btn')?'collection-content':'solution-content';const targetEl=document.getElementById(targetId);if(!targetEl){return}setTimeout(()=>{targetEl.scrollIntoView({behavior:'smooth',block:'start'})},200);}},true)})});function resolveToAbsolute(rel,base){try{return new URL(rel,base).href}catch(_){return rel;}}function initLogoLoader(){showLoader();const logoImg=document.querySelector('.logo img');if(logoImg){watchLogo(logoImg)}else{const observer=new MutationObserver((records,obs)=>{const img=document.querySelector('.logo img');if(img){watchLogo(img);obs.disconnect()}});observer.observe(document.documentElement,{childList:true,subtree:true});setTimeout(hideLoader,5000)}}if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',initLogoLoader)}else{initLogoLoader()}let menuNavInProgress=false;document.addEventListener("DOMContentLoaded",()=>{console.log("[INIT] DOMContentLoaded");document.documentElement.classList.add('ajax-loading');loadHeaderFooter().then(()=>{console.log("[INIT] Header/Footer loaded");initCollectionLoader('.collection-btn','#collection-content');initCollectionLoader('.solution-btn','#solution-content');initPjaxNavigation();const hash=window.location.hash;if(hash&&hash!=="#"){requestAnimationFrame(()=>{scrollToSection(hash)})}}).catch(err=>console.error('[INIT] Header/Footer load failed:',err)).finally(()=>{document.documentElement.classList.remove('ajax-loading');console.log("[INIT] ajax-loading class removed")})});let disableHeaderAutoHide=false;function disableHeaderHideUntilScrollEnd(){disableHeaderAutoHide=true;console.log("[HEADER] Auto-hide disabled until scroll end");let scrollEndTimeout;function onScroll(){clearTimeout(scrollEndTimeout);scrollEndTimeout=setTimeout(()=>{disableHeaderAutoHide=false;window.removeEventListener("scroll",onScroll);console.log("[HEADER] Auto-hide re-enabled after scroll")},100)}window.addEventListener("scroll",onScroll)}let _scrollLocked=false;let _savedScrollPos=0;function applyScrollLock(shouldLock){if(shouldLock&&!_scrollLocked){_savedScrollPos=window.pageYOffset||document.documentElement.scrollTop;document.body.style.top=`-${ _savedScrollPos }px`;document.body.classList.add('no-scroll');_scrollLocked=true;console.log("[SCROLLLOCK] Scroll locked at pos",_savedScrollPos)}else if(!shouldLock&&_scrollLocked){document.body.classList.remove('no-scroll');document.body.style.top='';window.scrollTo(0,_savedScrollPos);_scrollLocked=false;console.log("[SCROLLLOCK] Scroll unlocked, restored to",_savedScrollPos)}}function syncScrollLockWithHamburger(){const hb=document.querySelector('.hamburger');const shouldLock=hb&&hb.classList.contains('open');if(shouldLock){lockScrollPreservePosition()}else{unlockScrollRestorePosition()}console.log("[SCROLLLOCK] Hamburger sync – locked?",shouldLock)}function syncScrollLockWithNav(){const navLinks=document.querySelector('.nav-links');if(!navLinks){return}if(navLinks.classList.contains('active')){lockScrollPreservePosition()}else{unlockScrollRestorePosition()}console.log("[SCROLLLOCK] Sync with nav:",navLinks.classList.contains('active'))}let _lockedScrollPos=0;let _scrollLockDepth=0;function lockScrollPreservePosition(){if(_scrollLockDepth===0){_lockedScrollPos=window.pageYOffset||document.documentElement.scrollTop;document.body.style.top=`-${ _lockedScrollPos }px`;document.body.classList.add('no-scroll');document.documentElement.classList.add('no-scroll');console.log('[SCROLLLOCK] Locked at',_lockedScrollPos)}_scrollLockDepth+=1}function unlockScrollRestorePosition(){if(_scrollLockDepth===0){return}_scrollLockDepth-=1;if(_scrollLockDepth===0){document.body.classList.remove('no-scroll');document.documentElement.classList.remove('no-scroll');document.body.style.top='';requestAnimationFrame(()=>{window.scrollTo(0,_lockedScrollPos);console.log('[SCROLLLOCK] Restored scroll to',_lockedScrollPos)})}}const dropdownOriginalHtml=new Map();const menuStack=new Map();function initDropdowns(){document.querySelectorAll('.dropdown').forEach(dd=>{const toggle=dd.querySelector('.dropdown-toggle');const menu=dd.querySelector('.dropdown-menu');if(!toggle||!menu){return}const originalHtml=menu.innerHTML;let stack=[];toggle.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();const isOpening=!dd.classList.contains('open');if(isOpening){stack=[];menu.innerHTML=originalHtml;attachMenuHandlers(menu);if(!menu.querySelector('.show-all-main')){const rel=toggle.getAttribute('data-submenu')||toggle.getAttribute('href');let url;if(!rel||rel==='null'){url='/products/'}else{url=new URL(rel,window.location.href).href}const li=document.createElement('li');li.classList.add('show-all-main');li.innerHTML=`
  <button type="button" class="show-all-btn"
          onclick="window.location.href='${ url }'">
    SHOW ALL
  </button>`;menu.appendChild(li)}}dd.classList.toggle('open')});document.addEventListener('click',e=>{if(!dd.contains(e.target)&&dd.classList.contains('open')){dd.classList.remove('open')}});attachMenuHandlers(menu);function attachMenuHandlers(currentMenu){const backBtn=currentMenu.querySelector('.dropdown-back');if(backBtn){backBtn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();if(!stack.length){return}const{html}=stack.pop();currentMenu.innerHTML=html;attachMenuHandlers(currentMenu)})}currentMenu.querySelectorAll('a[data-submenu]').forEach(link=>{link.addEventListener('click',async e=>{e.preventDefault();e.stopPropagation();stack.push({html:currentMenu.innerHTML});currentMenu.innerHTML='<li class="loading">Loading…</li>';try{const rel=link.getAttribute('data-submenu');const submenuUrl=new URL(rel,window.location.href).href;const baseUrl=submenuUrl.endsWith('/')?submenuUrl:submenuUrl+'/';const itemsUrl=baseUrl+'items.json';const res=await fetch(itemsUrl);if(!res.ok){throw new Error(res.statusText)}const items=await res.json();const title=link.textContent.trim().toUpperCase();const backHtml=`
              <li class="back">
                <button type="button" class="dropdown-back">
                  ← Return from ${ title }
                </button>
              </li>`;const dirItems=items.filter(i=>i.type==='dir');const fileItems=items.filter(i=>i.href.endsWith('.html'));let html=backHtml;await Promise.all(dirItems.map(async dir=>{const dirUrl=new URL(dir.href,baseUrl).href;const dirJson=(dirUrl.endsWith('/')?dirUrl:dirUrl+'/')+'items.json';let childItems=[];try{const dres=await fetch(dirJson);if(dres.ok){childItems=await dres.json()}}catch(_){}if(childItems.length===0){html+=`<li>
                           <a href="${dir.href }"
                              class="disabled"
                              style="opacity:0.5; pointer-events:none;">
                             ${dir.name }
                           </a>
                         </li>`}else{html+=`<li>
                           <a href="${dir.href }" data-submenu="${dir.href }">
                             ${dir.name }
                           </a>
                         </li>`}}));fileItems.forEach(file=>{html+=`<li>
                         <a href="${file.href }">
                           ${file.name }
                         </a>
                       </li>`});html+=`
              <li class="show-all">
                <button type="button" class="dropdown-back"
                        onclick="window.location.href='${ submenuUrl }'">
                  SHOW ALL
                </button>
              </li>`;currentMenu.innerHTML=html;attachMenuHandlers(currentMenu)}catch(err){console.error('[DROPDOWN JSON] error:',err);currentMenu.innerHTML='<li class="error">Error loading categories</li>'}})})}})}function attachDropdownHandlers(menu,dd){const freshMenu=menu.cloneNode(true);menu.parentNode.replaceChild(freshMenu,menu);menu=freshMenu;const stack=menuStack.get(menu);const backBtn=menu.querySelector('.dropdown-back');if(backBtn){backBtn.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();if(stack.length===0){return}const prevHtml=stack.pop();menu.innerHTML=prevHtml;attachDropdownHandlers(menu,dd)})}menu.querySelectorAll('a[data-submenu]').forEach(link=>{link.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();stack.push(menu.innerHTML);loadDropdownJson(menu,link.getAttribute('data-submenu'),dd)})})}async function loadDropdownJson(menu,basePath,dd){menu.innerHTML='<li class="loading">Loading…</li>';try{const res=await fetch(basePath+'items.json');if(!res.ok){throw new Error(res.statusText)}const items=await res.json();const stack=menuStack.get(menu);const parentTitle=stack.length?stack[stack.length-1].match(/← Back to (.+)/)?.[1]||'':'';const backHtml=parentTitle?`<li class="back">
           <button type="button" class="dropdown-back">
             ← Back to ${ parentTitle }
           </button>
         </li>`:'';const itemsHtml=items.filter(i=>i.type==='dir'||i.href.endsWith('.html')).map(i=>{const attrs=i.type==='dir'?`data-submenu="${i.href }"`:'';return `<li>
                  <a href="${i.href }" ${ attrs }>
                    ${i.name }
                  </a>
                </li>`}).join('');menu.innerHTML=backHtml+itemsHtml;attachDropdownHandlers(menu,dd)}catch(err){console.error('[DROPDOWN JSON] error:',err);menu.innerHTML='<li class="error">Error loading categories</li>'}}function initHeaderFunctions(){const header=document.querySelector('.header');if(!header){return}let lastScrollY=window.scrollY;window.addEventListener('scroll',()=>{if(disableHeaderAutoHide){header.classList.remove('header-hidden');lastScrollY=window.scrollY;return}const now=window.scrollY;if(now>lastScrollY&&now>1){header.classList.add('header-hidden')}if(now<lastScrollY){header.classList.remove('header-hidden')}lastScrollY=now});const hamburger=header.querySelector('.hamburger');const navLinks=header.querySelector('.nav-links');if(hamburger&&navLinks){hamburger.addEventListener('click',e=>{e.preventDefault();e.stopPropagation();hamburger.classList.toggle('open');navLinks.classList.toggle('active');syncScrollLockWithHamburger()})}document.addEventListener('click',e=>{if(e.target.closest('.dropdown-menu')){return}if(navLinks.classList.contains('active')&&!navLinks.contains(e.target)&&!hamburger.contains(e.target)){navLinks.classList.remove('active');hamburger.classList.remove('open');syncScrollLockWithHamburger()}});header.querySelectorAll('.nav-links a, .dropdown a').forEach(link=>{link.addEventListener('click',()=>{if(link.hasAttribute('data-submenu')){return}navLinks.classList.remove('active');hamburger.classList.remove('open');header.querySelectorAll('.dropdown.open').forEach(dd=>dd.classList.remove('open'));syncScrollLockWithHamburger();header.classList.remove('header-hidden')})});header.addEventListener('click',e=>{const link=e.target.closest('.logo a, a.logo');if(!link){return}e.preventDefault();e.stopPropagation();closeAllMenus();showLoader();loadPageViaAjax(link.href,{replaceState:false}).then(()=>{const logoImg=document.querySelector('.logo img');if(logoImg){watchLogo(logoImg)}else{const obs=new MutationObserver((recs,o)=>{const img=document.querySelector('.logo img');if(img){watchLogo(img);o.disconnect()}});obs.observe(document.documentElement,{childList:true,subtree:true});setTimeout(hideLoader,5000)}}).catch(err=>{console.error('[HEADER] Logo PJAX error:',err);hideLoader()})});initDropdowns();initHeaderScrollHandlers()}function getCssVarPx(varName){const raw=getComputedStyle(document.documentElement).getPropertyValue(varName).trim();if(!raw){return 0}const div=document.createElement('div');div.style.position='absolute';div.style.visibility='hidden';div.style.height=raw;document.body.appendChild(div);const px=div.getBoundingClientRect().height;document.body.removeChild(div);console.log(`[SCROLL LOG] ${ varName } raw='${ raw }', measured=${ px }px`);return px}function getScrollOffset(){const headerHeight=getCssVarPx('--header-height');const scrollMargin=getCssVarPx('--scroll-margin');const total=headerHeight+scrollMargin;console.log(`[SCROLL LOG] total scroll-offset = header(${ headerHeight }px) + margin(${ scrollMargin }px) = ${ total }px`);return total}function scrollToSection(hash){console.log(`\n[SCROLL LOG] scrollToSection called with hash='${ hash }'`);if(!hash||hash==='#'){console.log("[SCROLL LOG] Empty or '#' hash: scrolling to top");window.scrollTo({top:0,behavior:'smooth'});return}const id=hash.charAt(0)==='#'?hash.slice(1):hash;const target=document.getElementById(id);if(!target){console.warn(`[SCROLL LOG] No element found with id='${ id }'`);return}const rectTop=target.getBoundingClientRect().top;const pageOffset=window.pageYOffset;console.log(`[SCROLL LOG] target.getBoundingClientRect().top=${ rectTop }px, window.pageYOffset=${ pageOffset }px`);const offset=getScrollOffset();const desiredScrollTop=rectTop+pageOffset-offset;console.log(`[SCROLL LOG] Calculated window.scrollTo top=${ desiredScrollTop }px`);window.scrollTo({top:desiredScrollTop,behavior:'smooth'})}function initHeaderScrollHandlers(){const header=document.querySelector('.header');if(!header){console.warn("[SCROLL LOG] initHeaderScrollHandlers: .header elementtiä ei löytynyt");return}const links=header.querySelectorAll('a[href*="#"]:not([data-submenu])');console.log(`[SCROLL LOG] initHeaderScrollHandlers: liitetään ${links.length } linkkiin`);links.forEach(link=>{link.addEventListener('click',e=>{const href=link.getAttribute('href');const hashIndex=href.indexOf('#');if(hashIndex===-1){return}const hash=href.slice(hashIndex);const path=window.location.pathname.replace(/(index\.html)?$/,'');const onHome=(path==='/'||path==='');closeAllMenus();disableHeaderHideUntilScrollEnd();unlockScrollRestorePosition();e.preventDefault();e.stopPropagation();requestAnimationFrame(()=>{if(onHome){scrollToSection(hash);console.log(`[SCROLL LOG] history.replaceState with hash='${ hash }'`);history.replaceState(null,'',hash)}else{console.log("[SCROLL LOG] PJAX-load home with scrollToHash");loadPageViaAjax('/',{replaceState:false,scrollToHash:hash})}})})})}function initFooterFunctions(){const btn=document.getElementById('scroll-btn');if(!btn){return}window.addEventListener('scroll',()=>{btn.style.display=window.pageYOffset>100?'block':'none'});btn.addEventListener('click',()=>{if(document.activeElement&&['INPUT','TEXTAREA','SELECT'].includes(document.activeElement.tagName)){document.activeElement.blur()}disableHeaderHideUntilScrollEnd();window.scrollTo({top:0,behavior:'smooth'});console.log("[FOOTER] Scroll-btn clicked, scroll to top")})}function closeAllMenus(){if(window.menuNavInProgress){return}const header=document.querySelector('.header');if(!header){return}const hamburger=header.querySelector('.hamburger');const navLinks=header.querySelector('.nav-links');const dropdowns=header.querySelectorAll('.dropdown');navLinks.classList.remove('active');hamburger?.classList.remove('open');dropdowns.forEach(dd=>dd.classList.remove('open'));syncScrollLockWithHamburger();setTimeout(()=>header.classList.remove('header-hidden'),0);console.log("[MENU] closeAllMenus called - all menus closed")}function computePrefix(){const parts=window.location.pathname.split('/').filter(Boolean);return parts.map(_=>'../').join('')}function loadHeaderFooter(){const prefix=computePrefix();console.log("[LOAD] Header/Footer load with prefix",prefix);const headerPromise=fetch(prefix+'header.html').then(r=>{if(!r.ok){throw new Error('Header load: '+r.status)}return r.text()}).then(html=>{let headerContainer=document.getElementById('site-header');if(!headerContainer){headerContainer=document.createElement('header');headerContainer.id='site-header';document.body.insertAdjacentElement('afterbegin',headerContainer)}headerContainer.innerHTML=html;initHeaderFunctions();initDynamicHash();console.log("[LOAD] Header loaded and initialized")});const footerPromise=fetch(prefix+'footer.html').then(r=>{if(!r.ok){throw new Error('Footer load: '+r.status)}return r.text()}).then(html=>{let footerContainer=document.getElementById('site-footer');if(!footerContainer){footerContainer=document.createElement('footer');footerContainer.id='site-footer';document.body.insertAdjacentElement('beforeend',footerContainer)}footerContainer.innerHTML=html;initFooterFunctions();console.log("[LOAD] Footer loaded and initialized")});return Promise.all([headerPromise,footerPromise])}function initPjaxNavigation(){document.body.addEventListener('click',e=>{const link=e.target.closest('a');if(!link){return}const href=link.getAttribute('href');if(!href||link.origin!==location.origin||href.startsWith('#')||link.hasAttribute('data-submenu')){return}const urlObj=new URL(link.href);const path=urlObj.pathname;if(path.endsWith('.html')||path.endsWith('/')){e.preventDefault();closeAllMenus();showLoader();console.log("[PJAX] Navigating to",link.href);loadPageViaAjax(link.href,{replaceState:false})}});window.addEventListener('popstate',()=>{console.log("[PJAX] popstate, loading",location.pathname);showLoader();loadPageViaAjax(location.pathname,{replaceState:true})})}function loadPageViaAjax(url,options={}){showLoader();document.documentElement.classList.add('ajax-loading');console.log("[PJAX] Loading page via AJAX",url);return fetch(url).then(r=>{if(!r.ok){throw new Error('Ajax load: '+r.status)}return r.text()}).then(htmlText=>{const doc=new DOMParser().parseFromString(htmlText,'text/html');const newContent=doc.getElementById('content')||doc.querySelector('main');if(!newContent){throw new Error('No content element found in '+url)}newContent.id='content';document.getElementById('content').replaceWith(newContent);newContent.querySelectorAll('img[src]').forEach(img=>{img.src=resolveToAbsolute(img.getAttribute('src'),url)});const t=doc.querySelector('title');if(t){document.title=t.textContent}const hashPart=options.scrollToHash||"";const finalUrl=url+hashPart;if(options.replaceState){history.replaceState({},"",finalUrl)}else{history.pushState({},"",finalUrl)}}).then(()=>{console.log("[PJAX] Re-initializing content scripts");initCollectionLoader('.collection-btn','#collection-content');initCollectionLoader('.solution-btn','#solution-content');initDynamicHash();}).then(()=>{hideLoader();document.documentElement.classList.remove('ajax-loading');document.body.classList.remove('no-scroll');document.body.style.top="";const hash=options.scrollToHash||window.location.hash;if(hash&&hash!=="#"){setTimeout(()=>scrollToSection(hash),0)}else{window.scrollTo({top:0,behavior:'instant'})}}).catch(err=>console.error('[PJAX] error:',err))}function updateActiveNavLink(){const path=location.pathname.split('/').pop()||'index.html';document.querySelectorAll('.nav-links a').forEach(a=>a.classList.toggle('active',a.getAttribute('href')===path));console.log("[PJAX] Updated active nav link:",path)}function initCollectionLoader(buttonSelector,targetSelector){const buttons=document.querySelectorAll(buttonSelector);const target=document.querySelector(targetSelector);if(!target){return}let currentURL=null;let activeBtn=null;target.addEventListener('transitionend',e=>{if(e.propertyName==='height'&&currentURL){target.style.height='auto'}});buttons.forEach(btn=>btn.addEventListener('click',async e=>{e.preventDefault();const url=btn.href;console.log("[COLLECTION] Button clicked:",url);if(currentURL===url){const isOpen=target.getBoundingClientRect().height>0;if(isOpen){const startH=target.getBoundingClientRect().height;target.style.height=`${ startH }px`;target.getBoundingClientRect();toggleHeight(0);currentURL=null;btn.classList.remove('active');activeBtn=null;console.log("[COLLECTION] Already open, closing collection")}else{toggleHeight(target.scrollHeight);currentURL=url;btn.classList.add('active');activeBtn=btn;console.log("[COLLECTION] Already closed, opening collection")}return}const oldH=target.getBoundingClientRect().height;target.style.height=`${ oldH }px`;target.getBoundingClientRect();let html='';try{const res=await fetch(url);if(!res.ok){throw new Error(res.statusText)}html=await res.text()}catch(err){console.error('[COLLECTION] Fetch error:',err);target.innerHTML='<p>Sorry, could not load content.</p>'}if(html){const doc=new DOMParser().parseFromString(html,'text/html');const main=doc.querySelector('main');target.innerHTML=main?main.innerHTML:'<p>Error: no <main> found</p>'}await new Promise(requestAnimationFrame);target.style.height='auto';const newH=target.scrollHeight;target.style.height=`${ oldH }px`;toggleHeight(newH);currentURL=url;if(activeBtn){activeBtn.classList.remove('active')}btn.classList.add('active');activeBtn=btn;console.log("[COLLECTION] New collection loaded:",url)}));function toggleHeight(toPx){target.getBoundingClientRect();requestAnimationFrame(()=>target.style.height=`${ toPx }px`)}}function loadItems(){console.log('[ITEMS] Dynamic load disabled – static links already in HTML :D')}document.addEventListener('DOMContentLoaded',()=>{const c=document.getElementById('items-container');if(c&&window.initialItemsJson){loadItems()}});function initDynamicHash(){const header=document.querySelector('.header');if(!header){return}const sections=document.querySelectorAll("section[id]:not([id='hero'])");if(!sections.length){return}let lastHash=window.location.hash;const opts={root:null,rootMargin:`-${header.offsetHeight }px 0px -50% 0px`,threshold:0};const observer=new IntersectionObserver(entries=>{entries.forEach(entry=>{if(entry.isIntersecting){const newHash=`#${entry.target.id }`;if(lastHash!==newHash){history.replaceState(null,'',newHash);lastHash=newHash;console.log("[HASH] Section intersected, updated hash to",newHash)}}})},opts);sections.forEach(s=>observer.observe(s));window.addEventListener('scroll',()=>{if(window.scrollY<=header.offsetHeight&&window.location.hash){history.replaceState(null,'',location.pathname+location.search);lastHash='';console.log("[HASH] At top, hash cleared")}})}function ensureViewportMeta(){if(!document.querySelector('meta[name="viewport"]')){const meta=document.createElement('meta');meta.name='viewport';meta.content='width=device-width, initial-scale=1';document.head.prepend(meta)}}document.addEventListener('DOMContentLoaded',ensureViewportMeta);async function loadItemsFromSitemap(){try{const resp=await fetch('/sitemap.xml');if(!resp.ok){throw 'sitemap not found'}const xmlText=await resp.text();const doc=new DOMParser().parseFromString(xmlText,'application/xml');const locEls=doc.getElementsByTagName('loc');const urls=[];for(let el of locEls){const loc=el.textContent.trim();if(/\/\d+\.html$/.test(loc)){urls.push(loc)}}return urls.map(parseItem)}catch(err){console.error('❌ loadItemsFromSitemap error:',err);return[]}}function parseItem(loc){const u=new URL(loc,window.location.origin);const seg=u.pathname.split('/').filter(Boolean);const last=seg.pop();return{id:last.replace(/\.html$/,''),name:seg[seg.length-1]||'',categories:seg.slice(0,seg.length-1),pathname:u.pathname}}function levenshtein(a,b){const m=a.length,n=b.length;if(!m){return n}if(!n){return m}const dp=Array(m+1).fill().map(()=>Array(n+1).fill(0));for(let i=0;i<=m;i+=1){dp[i][0]=i}for(let j=0;j<=n;j+=1){dp[0][j]=j}for(let i=1;i<=m;i+=1){for(let j=1;j<=n;j+=1){const cost=a[i-1]===b[j-1]?0:1;dp[i][j]=Math.min(dp[i-1][j]+1,dp[i][j-1]+1,dp[i-1][j-1]+cost)}}return dp[m][n]}function render(item){const li=document.createElement('li');const img=document.createElement('img');img.className='item-image';img.crossOrigin='anonymous';img.src=window.location.origin+item.pathname.replace(/\.html$/,'_1.avif');img.alt=item.name;li.appendChild(img);const txt=document.createElement('div');txt.className='item-text';const a=document.createElement('a');a.href=window.location.origin+item.pathname;a.textContent=`${item.name } (${item.id })`;a.className='item-title';txt.appendChild(a);if(item.categories.length){const pathDiv=document.createElement('div');pathDiv.className='item-path';pathDiv.textContent=item.categories.join(' > ');txt.appendChild(pathDiv)}li.appendChild(txt);return li}function doSearch(q,resultsEl,items){const term=q.trim().toLowerCase();resultsEl.innerHTML='';if(!term){return}const tokens=term.split(/\s+/);const hits=items.filter(item=>tokens.every(tok=>item.id.includes(tok)||item.name.toLowerCase().includes(tok)||item.categories.join(' ').toLowerCase().includes(tok)));if(hits.length){hits.forEach(i=>resultsEl.appendChild(render(i)))}else{const suggestions=items.map(item=>({item,score:tokens.map(tok=>Math.min(levenshtein(tok,item.id),levenshtein(tok,item.name.toLowerCase()),levenshtein(tok,item.categories.join(' ').toLowerCase()))).reduce((a,b)=>a+b,0)})).sort((a,b)=>a.score-b.score).slice(0,5).map(x=>x.item);if(suggestions.length){const header=document.createElement('li');header.textContent='Suggestions:';header.style.fontWeight='bold';header.style.margin='8px 0';resultsEl.appendChild(header);suggestions.forEach(i=>resultsEl.appendChild(render(i)))}else{const none=document.createElement('li');none.textContent='No results';none.className='no-results';resultsEl.appendChild(none)}}}function initLazySearch(){const holder=document.createElement('div');document.body.appendChild(holder);let items=[];let itemsLoaded=false;fetch('/search.html').then(r=>{if(!r.ok){throw new Error('search.html load failed: '+r.status)}return r.text()}).then(html=>{holder.innerHTML=html;const wrapper=holder.querySelector('.search-wrapper');const btn=wrapper.querySelector('.search-button');const input=wrapper.querySelector('.search-input');const results=wrapper.querySelector('.search-results');btn.addEventListener('click',e=>{e.stopPropagation();const isOpen=wrapper.classList.contains('open');const hasText=input.value.trim().length>0;if(isOpen&&!hasText){resetSearch();return}if(!itemsLoaded){loadItemsFromSitemap().then(data=>{items=data;itemsLoaded=true;console.log(`${items.length } items loaded from sitemap.`);openSearch()})}else{wrapper.classList.toggle('open');if(wrapper.classList.contains('open')){input.focus()}else{resetSearch()}}});document.addEventListener('click',e=>{if(wrapper.classList.contains('open')&&!wrapper.contains(e.target)){resetSearch()}});document.addEventListener('keydown',e=>{if(e.key==='Escape'&&wrapper.classList.contains('open')){resetSearch()}});input.addEventListener('input',()=>{if(!itemsLoaded){return}clearTimeout(input._searchTimer);input._searchTimer=setTimeout(()=>{doSearch(input.value,results,items)},200)});function openSearch(){wrapper.classList.add('open');input.focus()}function resetSearch(){wrapper.classList.remove('open');input.value='';results.innerHTML=''}}).catch(err=>{console.error('[LazySearch] init error:',err)})}document.addEventListener('DOMContentLoaded',initLazySearch);